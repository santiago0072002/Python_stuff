def array_sum(array_list): # define the function that takes a paremeter
    return sum(array_list) # return back to the caller everything that was passed to it added

list1 = [1,2,3,4,5]         # declare a list
print(array_sum(list1))     # caller, show it in the console. 