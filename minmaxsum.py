# hacker rank mix max me warming up. 
# 
def miniMaxSum(arr):# pass an array of numbers
    print(sum(arr) - max(arr), sum(arr) - min(arr)) # using python build it libraries you can just substract the max and get the min and substract the min to get the maximun. 

list2 = [1, 2, 3, 4 , 5]

miniMaxSum(list2)
